---

---

<section id="empresa" class="about-section source-container reveal">
  <aside>
    <h3>Somos una empresa de software enfocado en pequeñas y medianas empresa</h3>

    <ul>
      <li>Oaxaca, México</li>
      <li>Atención remota</li>
      <li>Sistemas a medida</li>
      <li>Automatización</li>
      <li>Soporte continuo</li>
    </ul>
  </aside>

  <div>
    <h2>
      ADNOVA nace para ayudar a negocios que ya no pueden seguir dependiendo de
      hojas de cálculo, procesos manuales y herramientas desconectadas.
    </h2>

    <a href="#contacto" class="dark-button">
      Sobre nosotros
      <span>↗</span>
    </a>
  </div>
</section>

<style>
  .about-section {
    display: grid;
    grid-template-columns: 0.25fr 0.75fr;
    gap: 80px;
    padding: 86px 34px;
    border-left: 1px solid var(--line);
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .about-section aside h3 {
    margin: 0 0 34px;
    font-size: 21px;
    line-height: 1.25;
    font-weight: 720;
    letter-spacing: -0.045em;
  }

  .about-section ul {
    list-style: square;
    padding-left: 18px;
    margin: 0;
    color: var(--ink);
    font-size: 18px;
    line-height: 1.55;
  }

  .about-section h2 {
    max-width: 1180px;
    margin: 0;
    font-size: var(--text-section);
    line-height: 1.04;
    font-weight: var(--weight-heading);
    letter-spacing: -0.06em;
  }
  font-size: var(--text-section);
  line-height: 1.04;
  font-weight: var(--weight-heading);
  letter-spacing: -0.06em;
  @media (max-width: 1100px) {
    .about-section {
      grid-template-columns: 1fr;
      gap: 52px;
    }
  }

  @media (max-width: 700px) {
    .about-section {
      padding: 72px 20px;
    }
  }
</style>
